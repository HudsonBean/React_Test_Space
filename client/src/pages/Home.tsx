import React from "react";
import { useGetAllUsersQuery, useGetUserQuery } from "../features/apiSlice";

export const Home = () => {
  //Get all users
  //   const { data, error, isLoading } = useGetAllUsersQuery(undefined);
  //   console.log(data);
  //Get a user
  //   const { data, error, isLoading } = useGetUserQuery(25);
  //   console.log(data);
  return <div>Home</div>;
};
